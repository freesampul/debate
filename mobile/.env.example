EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# ngrok: the TUI truncates the URL — do not copy from the terminal. Run `npm run sync-ngrok` in mobile/
# (reads http://127.0.0.1:4040) or open http://127.0.0.1:4040 for the full https://….ngrok-free.app URL.
# Physical device + Expo Go: use HTTPS (ngrok); iOS blocks http://192.168.x.x to your Mac.
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_LIVEKIT_URL=
